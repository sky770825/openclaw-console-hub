# Email 範本系統 技術規格

## 任務資訊
- **任務 ID**: t1770909565424
- **名稱**: [Kimi] Email 範本系統
- **描述**: 動態範本引擎、變數替換、HTML/Plain Text
- **執行時間**: 2026-02-13T03:06:58+08:00
- **狀態**: ✅ 完成

## 功能規格

### 核心功能
1. **範本引擎**
   - Handlebars/Mustache 語法支援
   - 條件判斷 (if/else)
   - 迴圈迭代 (each)
   - 自定義 Helper 函數

2. **變數替換**
   - 物件路徑支援 (user.name)
   - 預設值設定
   - 日期格式化
   - 字串處理函數

3. **雙格式輸出**
   - HTML 豐富格式
   - Plain Text 純文字
   - 自動 HTML → Text 轉換
   - Inline CSS 支援

4. **範本管理**
   - 檔案式範本儲存
   - 範本繼承與組合
   - 預覽功能
   - 版本控制

### API 設計
```typescript
interface TemplateEngine {
  render(template: string, data: object): RenderResult;
  renderFile(templatePath: string, data: object): RenderResult;
  registerHelper(name: string, fn: Function): void;
}

interface RenderResult {
  html: string;
  text: string;
  subject: string;
}

// 使用範例
const result = await emailTemplate.render('welcome.hbs', {
  user: { name: '老蔡', email: 'andy@example.com' },
  activationLink: 'https://...',
  expiryDate: new Date()
});
```

### 範本範例
```handlebars
{{! welcome.hbs }}
<p>Hi {{user.name}},</p>
<p>歡迎加入！請點擊 <a href="{{activationLink}}">這裡</a> 啟動帳號。</p>
<p>連結有效至 {{formatDate expiryDate "YYYY-MM-DD"}}</p>
```

## 相依套件
- `handlebars` - 範本引擎
- `html-to-text` - HTML 轉純文字
- `juice` - CSS Inline

---
*由 Autopilot 自動生成*
