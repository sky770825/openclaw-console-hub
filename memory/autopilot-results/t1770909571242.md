# Google Sheets Skill 技術規格

## 任務資訊
- **任務 ID**: t1770909571242
- **任務名稱**: [CoDEX] Google Sheets Skill
- **執行時間**: 2026-02-13T02:03:00+08:00
- **執行 Agent**: CoDEX (技術規格撰寫)

---

## 1. 功能概述

Google Sheets Skill 提供：
- 試算表讀寫操作
- 資料格式化
- 圖表建立與管理
- 批次處理
- 公式操作

## 2. 核心功能

### 2.1 試算表操作
```typescript
interface SpreadsheetOperations {
  // 建立試算表
  create(title: string): Promise<Spreadsheet>
  
  // 開啟試算表
  open(spreadsheetId: string): Promise<Spreadsheet>
  
  // 複製試算表
  copy(spreadsheetId: string, newTitle: string): Promise<Spreadsheet>
  
  // 刪除試算表
  delete(spreadsheetId: string): Promise<void>
  
  // 分享設定
  share(spreadsheetId: string, email: string, role: 'reader' | 'writer' | 'owner'): Promise<void>
}
```

### 2.2 工作表操作
```typescript
interface SheetOperations {
  // 新增工作表
  addSheet(title: string): Promise<Sheet>
  
  // 刪除工作表
  deleteSheet(sheetId: number): Promise<void>
  
  // 重新命名
  renameSheet(sheetId: number, newTitle: string): Promise<void>
  
  // 複製工作表
  duplicateSheet(sheetId: number, newTitle: string): Promise<Sheet>
  
  // 隱藏/顯示
  hideSheet(sheetId: number): Promise<void>
  showSheet(sheetId: number): Promise<void>
}
```

### 2.3 資料讀寫
```typescript
interface DataOperations {
  // 讀取範圍
  read(range: string): Promise<any[][]>
  
  // 讀取為物件陣列 (使用首行為欄位名)
  readAsObjects(range: string): Promise<Record<string, any>[]>
  
  // 寫入資料
  write(range: string, values: any[][]): Promise<void>
  
  // 追加資料
  append(values: any[][]): Promise<void>
  
  // 清除範圍
  clear(range: string): Promise<void>
  
  // 批次更新
  batchUpdate(updates: BatchUpdate[]): Promise<void>
}

interface BatchUpdate {
  range: string;
  values: any[][];
}
```

### 2.4 格式化
```typescript
interface FormattingOperations {
  // 設定儲存格格式
  setCellFormat(range: string, format: CellFormat): Promise<void>
  
  // 設定欄寬
  setColumnWidth(column: string | number, width: number): Promise<void>
  
  // 設定列高
  setRowHeight(row: number, height: number): Promise<void>
  
  // 設定條件格式
  setConditionalFormat(range: string, rules: ConditionalFormatRule[]): Promise<void>
  
  // 套用表格樣式
  applyTableStyle(range: string, style: TableStyle): Promise<void>
}

interface CellFormat {
  numberFormat?: { type: string; pattern?: string };
  backgroundColor?: { red: number; green: number; blue: number };
  textFormat?: {
    bold?: boolean;
    italic?: boolean;
    fontSize?: number;
    foregroundColor?: { red: number; green: number; blue: number };
  };
  horizontalAlignment?: 'LEFT' | 'CENTER' | 'RIGHT';
  verticalAlignment?: 'TOP' | 'MIDDLE' | 'BOTTOM';
  wrapStrategy?: 'OVERFLOW_CELL' | 'LEGACY_WRAP' | 'CLIP' | 'WRAP';
}
```

### 2.5 圖表操作
```typescript
interface ChartOperations {
  // 建立圖表
  createChart(spec: ChartSpec): Promise<Chart>
  
  // 更新圖表
  updateChart(chartId: number, spec: ChartSpec): Promise<void>
  
  // 刪除圖表
  deleteChart(chartId: number): Promise<void>
  
  // 匯出圖表
  exportChart(chartId: number, format: 'png' | 'jpg'): Promise<Buffer>
}

type ChartType = 'LINE' | 'AREA' | 'COLUMN' | 'BAR' | 'PIE' | 'SCATTER' | 'COMBO';

interface ChartSpec {
  title: string;
  type: ChartType;
  dataRange: string;
  xAxis?: { title?: string };
  yAxis?: { title?: string };
  legendPosition?: 'BOTTOM' | 'LEFT' | 'RIGHT' | 'TOP' | 'NONE';
}
```

### 2.6 公式操作
```typescript
interface FormulaOperations {
  // 設定公式
  setFormula(cell: string, formula: string): Promise<void>
  
  // 讀取公式 (而非計算值)
  readFormulas(range: string): Promise<string[][]>
  
  // 自動填入
  autoFill(sourceRange: string, destinationRange: string): Promise<void>
  
  // 插入函數
  insertFunction(range: string, functionName: string, args: string[]): Promise<void>
}
```

## 3. 資料轉換

```typescript
interface DataTransform {
  // 排序
  sort(range: string, sortSpecs: SortSpec[]): Promise<void>
  
  // 篩選
  filter(range: string, criteria: FilterCriteria): Promise<void>
  
  // 資料驗證
  setDataValidation(range: string, rule: DataValidationRule): Promise<void>
  
  // 尋找與取代
  findAndReplace(range: string, find: string, replace: string): Promise<void>
}

interface SortSpec {
  column: number;
  ascending: boolean;
}
```

## 4. 技術實作

### 4.1 使用 Google Sheets API v4
```typescript
import { google } from 'googleapis';

const sheets = google.sheets({ version: 'v4', auth: oauth2Client });

// 讀取範例
const response = await sheets.spreadsheets.values.get({
  spreadsheetId: '...',
  range: 'Sheet1!A1:D10'
});
```

### 4.2 認證方式
- OAuth 2.0 (使用者授權)
- Service Account (伺服器端)
- API Key (唯讀公開資料)

## 5. OpenClaw 整合

```yaml
skill: google-sheets
version: 1.0.0
commands:
  - gsheets.create
  - gsheets.open
  - gsheets.read
  - gsheets.write
  - gsheets.append
  - gsheets.format
  - gsheets.createChart
  - gsheets.share
```

## 6. 配置範例

```json
{
  "googleSheets": {
    "auth": {
      "type": "service_account",
      "projectId": "my-project",
      "privateKey": "${GOOGLE_PRIVATE_KEY}",
      "clientEmail": "${GOOGLE_CLIENT_EMAIL}"
    },
    "defaultSpreadsheetId": "...",
    "defaultSheetName": "Sheet1",
    "batchSize": 1000
  }
}
```

## 7. 實作階段

### Phase 1: 基礎讀寫
- [ ] API 連接與認證
- [ ] 讀取範圍資料
- [ ] 寫入與更新

### Phase 2: 格式化
- [ ] 儲存格格式
- [ ] 條件格式
- [ ] 欄寬/列高調整

### Phase 3: 進階功能
- [ ] 圖表建立
- [ ] 公式操作
- [ ] 批次處理

---

**狀態**: ✅ 技術規格完成
**下一步**: 進入實作階段 (Phase 1)
