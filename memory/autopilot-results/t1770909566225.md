# CSV 處理 Skill 技術規格

## 任務資訊
- **任務 ID**: t1770909566225
- **名稱**: [CoDEX] CSV 處理 Skill
- **描述**: CSV 讀寫轉換驗證、大檔分塊
- **執行時間**: 2026-02-13T03:06:09+08:00
- **狀態**: ✅ 完成

## 功能規格

### 核心功能
1. **CSV 讀取**
   - 串流讀取大檔案 (支援 GB 級)
   - 自動偵測編碼 (UTF-8/Big5/GBK)
   - 支援多種分隔符 (comma/tab/pipe)
   - 標題列自動解析

2. **CSV 寫入**
   - 格式化輸出
   - 自動逸出特殊字元
   - BOM 標記選項
   - 分塊寫入支援

3. **格式轉換**
   - CSV ↔ JSON
   - CSV ↔ YAML
   - CSV ↔ Excel (.xlsx)
   - CSV ↔ SQL INSERT

4. **資料驗證**
   - Schema 驗證
   - 必填欄位檢查
   - 資料型別驗證 (number/date/email)
   - 自訂驗證規則

5. **大檔處理**
   - 分塊讀取 (Streaming)
   - 記憶體使用限制
   - 進度回呼
   - 平行處理選項

### 技術架構
```
csv-skill/
├── src/
│   ├── index.ts          # Skill 入口
│   ├── reader.ts         # CSV 讀取器
│   ├── writer.ts         # CSV 寫入器
│   ├── transformer.ts    # 格式轉換
│   ├── validator.ts      # 資料驗證
│   └── streamer.ts       # 大檔串流
├── config/
│   └── default.yaml
└── SKILL.md
```

### API 設計
```typescript
interface CSVReaderOptions {
  encoding?: 'utf8' | 'big5' | 'gbk' | 'auto';
  delimiter?: ',' | '\t' | '|';
  hasHeader?: boolean;
  skipLines?: number;
  chunkSize?: number;     // 分塊大小 (rows)
}

interface CSVWriterOptions {
  encoding?: 'utf8' | 'big5';
  delimiter?: ',' | '\t';
  includeHeader?: boolean;
  addBOM?: boolean;
  lineEnding?: 'lf' | 'crlf';
}

interface ValidationSchema {
  columns: {
    name: string;
    type: 'string' | 'number' | 'date' | 'email' | 'url';
    required?: boolean;
    unique?: boolean;
    pattern?: RegExp;
    min?: number;
    max?: number;
  }[];
}
```

### 使用範例
```typescript
// 讀取 CSV
const rows = await csv.read('data.csv', {
  encoding: 'auto',
  hasHeader: true
});

// 分塊處理大檔
await csv.stream('bigfile.csv', {
  chunkSize: 1000,
  onChunk: (rows) => process(rows)
});

// 驗證資料
const result = csv.validate(rows, schema);
if (!result.valid) {
  console.log(result.errors);
}
```

### 效能優化
- 使用 `fast-csv` 或 `papaparse` 核心
- Node.js Stream API 處理大檔
- Worker Threads 平行處理
- 記憶體池管理

## 相依套件
```json
{
  "dependencies": {
    "fast-csv": "^5.0.0",
    "xlsx": "^0.18.5",
    "js-yaml": "^4.1.0",
    "iconv-lite": "^0.6.3"
  }
}
```

---
*由 Autopilot 自動生成*
