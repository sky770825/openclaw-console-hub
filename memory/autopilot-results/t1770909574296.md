# 語音轉文字 Skill 技術規格書

**任務 ID**: t1770909574296  
**執行時間**: 2026-02-13 01:02  
**類型**: 開發類（Cursor）

---

## 一、需求概述

集成 OpenAI Whisper API 和本地 Whisper 模型，提供即時語音轉錄能力。

---

## 二、API 設計

### 核心介面
```typescript
interface WhisperSkillConfig {
  provider: 'openai' | 'local';
  apiKey?: string;           // OpenAI API Key
  model?: string;            // 本地模型名稱
  language?: string;         // 語言代碼 (zh, en, etc)
  prompt?: string;           // 提示詞
  temperature?: number;      // 0-1
}

interface TranscriptionResult {
  text: string;
  language: string;
  confidence: number;        // 0-1
  segments: Segment[];
  duration: number;          // 秒
}

interface Segment {
  start: number;
  end: number;
  text: string;
}
```

### 主要方法
```typescript
transcribe(audioPath: string, config: WhisperSkillConfig): Promise<TranscriptionResult>
transcribeStream(audioStream: ReadableStream, config: WhisperSkillConfig): AsyncIterator<TranscriptionResult>
translateToEnglish(audioPath: string, config: WhisperSkillConfig): Promise<string>
```

---

## 三、實現細節

### OpenAI 提供商
- 依賴: `openai >= 4.0`
- 支援格式: mp3, mp4, mpeg, mpga, m4a, wav, webm
- 最大檔案: 25MB
- 成本: $0.006 / min

### 本地提供商
- 依賴: `openai-whisper`
- 支援模型: tiny, base, small, medium, large
- 推薦: small 平衡速度/品質
- 成本: $0 (本地執行)

---

## 四、實現步驟

### Phase 1: OpenAI 整合 (Day 1)
- [ ] 基礎 API 包裝
- [ ] 錯誤處理
- [ ] 單元測試

### Phase 2: 本地模型支援 (Day 2)
- [ ] Ollama 集成
- [ ] 模型管理
- [ ] 效能優化

### Phase 3: 流式處理 (Day 3)
- [ ] 串流音訊支援
- [ ] 即時轉錄回調
- [ ] 進度追蹤

---

## 五、測試計畫

```bash
# 單位測試
npm run test:whisper

# 整合測試
npm run test:whisper:integration

# 效能基準
npm run bench:whisper
```

---

*由 Autopilot Agent 自動生成*
