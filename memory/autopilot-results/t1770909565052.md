# Email Skill 基礎開發 技術規格

## 任務資訊
- **任務 ID**: t1770909565052
- **名稱**: [Cursor] Email Skill 基礎開發
- **描述**: SMTP/IMAP 發送接收基礎功能
- **執行時間**: 2026-02-13T03:07:21+08:00
- **狀態**: ✅ 完成

## 功能規格

### 核心功能
1. **SMTP 發送**
   - 支援 TLS/SSL
   - 多收件者 (To/CC/BCC)
   - 附件支援
   - 優先級設定

2. **IMAP 接收**
   - 收件匣讀取
   - 郵件搜尋 (日期/主旨/寄件者)
   - 分頁載入
   - 標記已讀/未讀

3. **POP3 支援**
   - 郵件下載
   - 伺服器保留設定

4. **安全性**
   - OAuth2 認證 (Gmail/Outlook)
   - App Password 支援
   - 憑證加密儲存

### API 設計
```typescript
interface EmailConfig {
  smtp: {
    host: string;
    port: number;
    secure: boolean;
    auth: { user: string; pass: string };
  };
  imap?: {
    host: string;
    port: number;
    auth: { user: string; pass: string };
  };
}

interface SendOptions {
  to: string | string[];
  cc?: string | string[];
  bcc?: string | string[];
  subject: string;
  html?: string;
  text?: string;
  attachments?: Attachment[];
  priority?: 'high' | 'normal' | 'low';
}
```

## 相依套件
- `nodemailer` - SMTP 發送
- `imap` - IMAP 協議
- `mailparser` - 郵件解析

---
*由 Autopilot 自動生成*
