# S-12 InsightPulse Web Dashboard - 技術規格

## 技術架構
```
┌─────────────────────────────────────────┐
│  Next.js 14 App Router                  │
│  - React Server Components              │
│  - Tailwind CSS + shadcn/ui             │
├─────────────────────────────────────────┤
│  State Management                       │
│  - Zustand (client state)               │
│  - React Query (server state)           │
├─────────────────────────────────────────┤
│  Visualization                          │
│  - Recharts (charts)                    │
│  - D3.js (complex viz)                  │
│  - Tremor (dashboard components)        │
├─────────────────────────────────────────┤
│  Backend                                │
│  - Next.js API Routes                   │
│  - Prisma ORM                           │
│  - PostgreSQL                           │
└─────────────────────────────────────────┘
```

## 核心頁面

### 1. Overview Dashboard
```tsx
// app/dashboard/page.tsx
export default function Dashboard() {
  return (
    <div className="grid grid-cols-12 gap-4">
      <MetricsCards className="col-span-12" />
      <SentimentChart className="col-span-8" />
      <TopTopics className="col-span-4" />
      <RecentAlerts className="col-span-6" />
      <CompetitorActivity className="col-span-6" />
    </div>
  )
}
```

### 2. Reports 頁面
- 日報/週報列表
- 搜尋與過濾
- PDF 匯出
- 分享連結

### 3. Settings 頁面
- 監控關鍵字設定
- 通知偏好
- 團隊成員管理
- API Key 管理

## 關鍵元件
```tsx
// components/SentimentChart.tsx
import { LineChart, Line, XAxis, YAxis } from 'recharts'

export function SentimentChart({ data }) {
  return (
    <LineChart data={data}>
      <Line type="monotone" dataKey="positive" stroke="#22c55e" />
      <Line type="monotone" dataKey="negative" stroke="#ef4444" />
      <XAxis dataKey="date" />
      <YAxis />
    </LineChart>
  )
}
```

## 性能優化
- 圖表虛擬滾動 (大數據量)
- 圖片 Lazy loading
- API Response 快取 (Redis)
- Edge Functions 部署

---
任務狀態: ✅ 技術規格完成
