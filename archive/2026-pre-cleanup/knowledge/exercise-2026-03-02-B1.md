# 練習題 B1：API 路由定義清單

經由 grep 掃描 server/src/routes/openclaw-tasks.ts，確認以下路由：
- GET /api/openclaw/tasks (列表)
- POST /api/openclaw/tasks (建立)
- PATCH /api/openclaw/tasks/:id (更新)
- DELETE /api/openclaw/tasks/:id (刪除)

這驗證了任務板的核心 CRUD 功能架構。