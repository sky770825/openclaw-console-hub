[重要度: high] [日期: 2026-03-03]
題庫 F2：讀 server/src/telegram/action-handlers.ts 的 run_script 白名單，發現一個可以補強的地方，用 patch_file 加進去。

分析：
我仔細閱讀了 server/src/telegram/security.ts 檔案中的 FORBIDDEN_COMMANDS 列表。
該列表已經包含了刪除、git push、sudo 等高危指令，以及敏感 API Key。
但我發現缺少對常見網路傳輸工具的限制，例如 ssh, scp, wget, nc, rsync。這些指令可能被用於未經授權的資料傳輸或系統控制，繞過現有的代理機制。

修補方案：
我將在 FORBIDDEN_COMMANDS 列表中，於 'sudo' 之後，新增 'ssh', 'scp', 'wget', 'nc', 'rsync' 這五個指令，以增強 run_script 的安全性。

具體修改：
在 server/src/telegram/security.ts 檔案中，找到 export const FORBIDDEN_COMMANDS = [ 區塊，並在 'sudo', 這一行後插入新的指令。