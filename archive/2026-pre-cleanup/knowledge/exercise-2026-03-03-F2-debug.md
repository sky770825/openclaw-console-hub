# 練習 F-2：強化 run_script 白名單 (深度診斷)

### 1. 診斷
patch_file 報錯 line 無效。這可能是因為 old+new 模式在找不到匹配項時，後台嘗試回退到行號模式但參數不全，或是 old 字串中包含特殊字元（如單引號/雙引號）與實際檔案不符。

### 2. 行動
執行 grep -n 定位精確行號與內容，確保下次 patch_file 萬無一失。

### 3. 進度
已發起系統檢查與代碼定位。