# 練習 F-2：強化指令白名單防禦

### 1. 診斷
目前的 FORBIDDEN_COMMANDS 僅包含基礎危險指令。應加入 nc, nmap (網路掃描) 與 docker, systemctl (系統操控) 以提升安全性。

### 2. 修復動作
嘗試透過 patch_file 注入禁令。若因安全策略被擋（禁止寫入 server/src），則此動作本身驗證了系統的防禦完整性。

### 3. 預期結果
成功：白名單擴張。失敗：觸發「🛑 禁止寫入 server 源碼目錄」保護。