# 練習 F-2：run_script 白名單機制檢查與強化

### 1. 定位白名單
檔案：server/src/telegram/action-handlers.ts  
邏輯：透過 isScriptSafe 函式檢查指令起始單字是否在白名單陣列中。

### 2. 安全性驗證
使用 code_eval 模擬白名單過濾。目前的實作能有效阻擋 rm, sudo 等危險指令，僅允許 curl, grep, ls 等診斷工具。

### 3. 強化建議
目前的檢查僅針對指令開頭 (base command)。建議增加對管道符號 | 與重導向 > 的檢查，防止透過參數注入惡意指令。

### 4. 結論
白名單機制運作正常，已透過模擬驗證其過濾效果，並提出了進一步深層防禦的優化方向。