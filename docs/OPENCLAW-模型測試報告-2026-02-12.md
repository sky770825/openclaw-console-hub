# OpenClaw 模型設定與測試報告（2026-02-12）

## 1. 狀態總覽

| 項目 | 結果 |
|------|------|
| Gateway | 運行中 (pid 6607)，RPC probe ok，port 18789 |
| 設定檔 | `~/.openclaw/openclaw.json`，權限已設為 600 |
| Primary 模型 | kimi/kimi-k2.5 |
| Fallbacks | kimi/kimi-k2-turbo-preview → anthropic/claude-sonnet-4-5-20250929 → anthropic/claude-haiku-4-5-20251001 |
| 已配置模型數 | 12（Kimi 2、Google 3、Anthropic 3、Ollama 4） |

---

## 2. 認證狀態（openclaw models status）

- **anthropic**：有效（env + openclaw.json apiKey 皆有，LaunchAgent 可用）
- **google**：有效（env / models.json）
- **kimi**：有效（profiles / models.json）
- **ollama**：有效（local）
- **google-antigravity**：OAuth ok（sky19880825@gmail.com）

---

## 3. 實測結果

- **Primary 測試**：`openclaw agent --agent main --local --message "回覆一個字：好"`
  - 結果：Kimi 正確回覆「好」，耗時約 8.7 秒。

---

## 4. Doctor 提示（非錯誤）

- **其他 LaunchAgent**：偵測到多個與 gateway 相關的 plist（如 ai.ollama.bot2、com.openclaw.gateway 等），目前實際使用的是 **ai.openclaw.gateway**。
- **Security**：Gateway 綁定 `lan` (0.0.0.0)，請確保 token 強度足夠；可執行 `openclaw security audit --deep`。
- **memory-core**：外掛未安裝或未標記為 memory，不影響模型對話。
- **WhatsApp**：未連結，不影響 Telegram / 模型。

---

## 5. 建議（可選）

1. **重複 plist**：若已確定只使用 `openclaw gateway` 安裝的 **ai.openclaw.gateway**，可手動卸載舊的以免混淆：
   ```bash
   launchctl bootout gui/$UID/com.openclaw.gateway 2>/dev/null
   rm ~/Library/LaunchAgents/com.openclaw.gateway.plist   # 確認後再刪
   ```
2. **定期**：換 API Key 時記得同時更新 `~/.openclaw/.env` 與 `openclaw.json` 內對應 provider 的 apiKey。

---

**結論**：模型設定與測試均正常，無需立即修改。
