# OpenClaw_reindex_knowledge

## 能力描述
全量重新掃描並重建向量資料庫，用於系統大規模更新或索引損壞時。

## 輸入參數
- mode: 'append' (增量) 或 'rebuild' (清空重建) (必填)

## 執行規範
- 異步執行：此操作較慢，會在背景運行，不應阻塞主進程。
- 頻率限制：禁止在短時間內連續觸發 rebuild。

## 輸出預期
啟動訊息與預計處理的檔案數。