# OpenClaw_write_file

## 能力描述
在指定路徑寫入檔案內容，支援自動目錄建立。這是 OpenClaw 產出成果與持久化記憶的核心手段。

## 輸入參數
- path: 檔案的絕對或相對路徑 (必填)
- content: 欲寫入的完整內容 (必填)

## 執行規範
1. 自動建立：若路徑中的目錄不存在，會自動建立。
2. 安全保護：禁止覆寫靈魂文件或敏感系統檔案。

## 輸出預期
JSON 包含 success, path, bytes_written。