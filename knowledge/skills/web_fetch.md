# OpenClaw_web_fetch

## 能力描述
讀取指定網頁的純文字內容，用於深度分析搜尋結果或讀取線上文檔。

## 輸入參數
- url: 網頁 URL (必填)

## 執行規範
- 內容清理：自動移除 HTML 標籤、JS 和 CSS，僅保留文字。
- 長度限制：回傳內容上限為 4000 字，避免 Context 溢出。
- SPA 限制：不支援 React/Vue 等前端渲染網站，僅限靜態內容。

## 輸出預期
網頁純文字內容。