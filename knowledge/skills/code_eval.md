# OpenClaw_code_eval

## 能力描述
在安全的沙盒環境中執行 JavaScript 邏輯，用於複雜數據處理、演算法計算或格式轉換。

## 輸入參數
- code: 欲執行的 JS 程式碼 (必填)

## 執行規範
- 隔離性：純邏輯執行，無法訪問檔案系統、網絡或外部模組 (禁止 import/require)。
- 限制：5 秒超時，記憶體受限。
- 回傳：必須使用 console.log 或回傳最後一個表達式的值。

## 輸出預期
執行結果的字串或 JSON 內容。