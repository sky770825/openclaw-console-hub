# Cursor AI Best Prompts & Modes (2026 更新)

## Agent Mode Prompts
1. **Refactor 全 codebase**：
   ```
   Cmd+K: \"Refactor all UI components to Tailwind + shadcn. Add tests. Plan first.\"
   ```

2. **Background Agent** (docs.cursor.com)：
   ```
   \"Build Kubernetes dashboard: clone repo → Ubuntu machine → install deps → Git branch → push PR\"
   ```

3. **Yolo / Shadow Workspace** (forum)：
   ```
   Enable Yolo: Autonomous refactor + accuracy
   vs GitHub Copilot Agent: Cursor 勝在 editor-native
   ```

4. **Bug Fix / Code Review**：
   ```
   \"AI Code Review: scan changes, find bugs, one-click fix. Run grep + test.\"
   ```

5. **Scaling Agents** (blog 2026)：
   ```
   \"Coordinate team: one agent refactor, one test, one docs. Avoid drift.\"
   ```

來源：cursor.com/features, forum.cursor.com, changelog v2.1 (rate limit 後補)