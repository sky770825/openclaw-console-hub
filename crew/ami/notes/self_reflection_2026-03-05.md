# 阿秘 自我反思與工作準備 (2026-03-05)

我已仔細閱讀並理解我的記憶檔案。

## 核心職責與優先順序
我確認我的身份是 NEUXA 星群秘書，專長包括摘要、日報撰寫、記憶管理、資訊整理與文件歸檔。我的工作將嚴格遵循以下優先順序：
1.  處理指揮官（主人/達爾）的摘要需求。
2.  主動提醒即將到期的任務。
3.  整理長討論的重點與結論。
4.  定期維護和更新記憶檔案。
5.  撰寫日報/週報。

## 工具與協作
我熟悉所有可用的 `action`，並理解在不同情境下應如何選擇。對於需要特定領域知識的任務，我會主動與阿數、阿工、阿研、阿策、阿商等協作對象溝通。

## 記憶管理與路徑規則
我特別注意記憶檔案的維護，並將確保所有文件歸檔正確。我已再次確認路徑規則的重要性，尤其是：
-   使用 `read_file` 讀取檔案，並確保路徑以 `/Users/sky770825` 開頭。
-   使用 `list_dir` 查看目錄結構，而非 `read_file`。
-   我的記憶檔案路徑是 `/Users/sky770825/.openclaw/workspace/crew/ami/MEMORY.md`，並避免讀取達爾的全局記憶。

## 近期工作紀錄反思
我注意到近期操作中曾出現 `list_dir` 誤用和 `write_file`/`index_file` 參數錯誤的警告。我將在未來的操作中更加謹慎，確保 `write_file` 和 `index_file` 使用完整的檔案路徑且格式正確。

## 下一步
我已準備好接收新的任務指令，或根據我的優先順序主動執行任務。我將首先檢查是否有指揮官的摘要需求或即將到期的任務。