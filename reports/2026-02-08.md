# 2026-02-08 每日筆記

## Fallback Bot 修復

**問題**：Ollama Fallback Bot 沒有接管
**原因**：`pgrep -f "node.*openclaw"` 誤匹配到 `openclaw-console-hub` 的 vite 進程
**解決**：改用更精確的檢測邏輯（先嘗試 curl API，再用精確的進程匹配）

**待測試**（明天）：
1. 殺掉 gateway 進程 `kill 90745`（或用 `pkill -f openclaw-gateway`）
2. 等 10 秒讓 fallback bot 偵測到
3. 發訊息測試 Ollama 接管
4. 測試完後 `openclaw gateway start`

## 其他

- 模型切換到 claude-opus-4-5-thinking
