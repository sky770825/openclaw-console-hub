# Discord Bot Skill 技術規格

## 任務資訊
- **任務 ID**: t1770909571583
- **任務名稱**: [CoDEX] Discord Bot Skill
- **執行時間**: 2026-02-13T02:02:00+08:00
- **執行 Agent**: CoDEX (技術規格撰寫)

---

## 1. 功能概述

Discord Bot Skill 提供：
- 訊息發送與管理
- 頻道操作 (建立/編輯/刪除)
- 角色管理
- 成員管理
- 互動元件 (按鈕/選單)

## 2. 核心功能

### 2.1 訊息操作
```typescript
interface MessageOperations {
  // 發送訊息
  send(channelId: string, content: string | MessageEmbed): Promise<Message>
  
  // 回覆訊息
  reply(messageId: string, content: string): Promise<Message>
  
  // 編輯訊息
  edit(messageId: string, newContent: string): Promise<Message>
  
  // 刪除訊息
  delete(messageId: string): Promise<void>
  
  // 發送嵌入訊息
  sendEmbed(channelId: string, embed: MessageEmbed): Promise<Message>
}

interface MessageEmbed {
  title?: string;
  description?: string;
  color?: number;
  fields?: { name: string; value: string; inline?: boolean }[];
  thumbnail?: { url: string };
  image?: { url: string };
  footer?: { text: string; iconURL?: string };
  timestamp?: Date;
}
```

### 2.2 頻道管理
```typescript
interface ChannelOperations {
  // 建立頻道
  create(guildId: string, name: string, type: ChannelType): Promise<Channel>
  
  // 編輯頻道
  edit(channelId: string, options: ChannelEditOptions): Promise<Channel>
  
  // 刪除頻道
  delete(channelId: string): Promise<void>
  
  // 設定權限
  setPermissions(channelId: string, roleId: string, permissions: Permission[]): Promise<void>
  
  // 建立分類
  createCategory(guildId: string, name: string): Promise<CategoryChannel>
}
```

### 2.3 角色管理
```typescript
interface RoleOperations {
  // 建立角色
  create(guildId: string, name: string, options?: RoleOptions): Promise<Role>
  
  // 編輯角色
  edit(roleId: string, options: RoleOptions): Promise<Role>
  
  // 刪除角色
  delete(roleId: string): Promise<void>
  
  // 賦予角色
  assign(memberId: string, roleId: string): Promise<void>
  
  // 移除角色
  remove(memberId: string, roleId: string): Promise<void>
}
```

### 2.4 成員管理
```typescript
interface MemberOperations {
  // 踢出成員
  kick(memberId: string, reason?: string): Promise<void>
  
  // 封禁成員
  ban(memberId: string, reason?: string, deleteDays?: number): Promise<void>
  
  // 解除封禁
  unban(memberId: string): Promise<void>
  
  // 設定暱稱
  setNickname(memberId: string, nickname: string): Promise<void>
  
  // 超時 (Timeout)
  timeout(memberId: string, duration: number, reason?: string): Promise<void>
}
```

## 3. 互動元件

### 3.1 按鈕
```typescript
interface ButtonComponent {
  type: 'button';
  style: 'primary' | 'secondary' | 'success' | 'danger' | 'link';
  label: string;
  customId?: string;
  url?: string;
  emoji?: string;
  disabled?: boolean;
}
```

### 3.2 選單
```typescript
interface SelectMenuComponent {
  type: 'select';
  customId: string;
  placeholder?: string;
  options: SelectOption[];
  minValues?: number;
  maxValues?: number;
  disabled?: boolean;
}

interface SelectOption {
  label: string;
  value: string;
  description?: string;
  emoji?: string;
  default?: boolean;
}
```

## 4. 事件監聽

```typescript
interface DiscordEvents {
  onMessage(callback: (message: Message) => void): void
  onReaction(callback: (reaction: Reaction) => void): void
  onMemberJoin(callback: (member: Member) => void): void
  onMemberLeave(callback: (member: Member) => void): void
  onSlashCommand(callback: (command: SlashCommand) => void): void
  onButtonClick(callback: (interaction: ButtonInteraction) => void): void
}
```

## 5. Slash Commands

```typescript
interface SlashCommandBuilder {
  name: string;
  description: string;
  options?: CommandOption[];
}

interface CommandOption {
  name: string;
  description: string;
  type: 'string' | 'integer' | 'boolean' | 'user' | 'channel' | 'role';
  required?: boolean;
  choices?: { name: string; value: string | number }[];
}
```

## 6. 技術實作

### 6.1 使用 discord.js
```typescript
import { Client, GatewayIntentBits } from 'discord.js';

const client = new Client({
  intents: [
    GatewayIntentBits.Guilds,
    GatewayIntentBits.GuildMessages,
    GatewayIntentBits.GuildMembers,
    GatewayIntentBits.MessageContent,
  ]
});
```

### 6.2 錯誤處理
```typescript
class DiscordError extends Error {
  code: string;
  constructor(code: string, message: string) {
    super(message);
    this.code = code;
  }
}

// 錯誤碼
enum DiscordErrorCode {
  INVALID_TOKEN = 'DISCORD_001',
  MISSING_PERMISSIONS = 'DISCORD_002',
  CHANNEL_NOT_FOUND = 'DISCORD_003',
  RATE_LIMITED = 'DISCORD_004',
}
```

## 7. OpenClaw 整合

```yaml
skill: discord
version: 1.0.0
commands:
  - discord.connect      # 連接 Discord
  - discord.disconnect   # 斷開連接
  - discord.send         # 發送訊息
  - discord.sendEmbed    # 發送嵌入訊息
  - discord.createChannel
  - discord.manageRole
  - discord.manageMember
```

## 8. 配置範例

```json
{
  "discord": {
    "token": "${DISCORD_BOT_TOKEN}",
    "defaultGuildId": "123456789",
    "permissions": [
      "SendMessages",
      "ManageChannels",
      "ManageRoles",
      "KickMembers",
      "BanMembers"
    ],
    "rateLimit": {
      "requestsPerSecond": 5
    }
  }
}
```

## 9. 實作階段

### Phase 1: 基礎連接
- [ ] Bot 連接與認證
- [ ] 基本訊息發送
- [ ] 錯誤處理

### Phase 2: 訊息管理
- [ ] 嵌入訊息
- [ ] 檔案上傳
- [ ] 訊息編輯/刪除

### Phase 3: 頻道與角色
- [ ] 頻道操作
- [ ] 角色管理
- [ ] 權限設定

### Phase 4: 進階功能
- [ ] Slash Commands
- [ ] 互動元件 (按鈕/選單)
- [ ] 事件監聽

---

**狀態**: ✅ 技術規格完成
**下一步**: 進入實作階段 (Phase 1)
