# 圖片生成 Skill 整合技術規格書

**任務 ID**: t1770909573837  
**執行時間**: 2026-02-13 01:02  
**類型**: 開發類（Cursor）

---

## 一、需求概述

支持 OpenAI DALL-E 3、Stable Diffusion 和本地圖生模型，提供統一的圖片生成 API。

---

## 二、API 設計

### 核心介面
```typescript
interface ImageGenerationConfig {
  provider: 'openai' | 'stablediffusion' | 'local';
  apiKey?: string;           // 服務 API Key
  model?: string;            // 模型名稱
  size?: '256x256' | '512x512' | '1024x1024';
  quality?: 'standard' | 'hd';
  style?: 'natural' | 'vivid';
}

interface GenerationResult {
  imageUrl: string;
  b64_json?: string;
  revisionNumber?: string;
  seed?: number;
  prompt: string;           // 實際使用的提示詞
  generationTime: number;    // 毫秒
}
```

### 主要方法
```typescript
generate(prompt: string, config: ImageGenerationConfig): Promise<GenerationResult>
generateVariation(imagePath: string, config: ImageGenerationConfig): Promise<GenerationResult>
editImage(imagePath: string, maskPath: string, prompt: string, config: ImageGenerationConfig): Promise<GenerationResult>
batchGenerate(prompts: string[], config: ImageGenerationConfig): Promise<GenerationResult[]>
```

---

## 三、實現細節

### OpenAI DALL-E 3
- 依賴: `openai >= 4.0`
- 尺寸: 1024x1024 (HD), 1792x1024, 1024x1792
- 成本: $0.04-0.08 / image
- 品質: 最佳 (但較慢)

### Stable Diffusion
- 依賴: `stability-ai`
- 尺寸: 512x512 - 1024x1024
- 成本: $0.01-0.03 / image
- 品質: 良好 (速度快)

### 本地模型 (Ollama)
- 模型: `stable-diffusion`, `sdxl`
- 成本: $0 (本地執行)
- 品質: 中等 (取決於硬體)

---

## 四、實現步驟

### Phase 1: DALL-E 基礎 (Day 1-2)
- [ ] 提示詞最佳化
- [ ] 圖片預覽
- [ ] 成本追蹤

### Phase 2: Stable Diffusion (Day 2-3)
- [ ] API 整合
- [ ] 模型管理
- [ ] 批量生成

### Phase 3: 本地支援 (Day 3-4)
- [ ] Ollama 集成
- [ ] 硬體適配
- [ ] 效能優化

---

## 五、Prompt 工程

### 最佳實踐範本
```
{描述} + {風格} + {品質指示}

範例:
"A serene mountain landscape, oil painting style, 
ultra-detailed, 4K resolution, trending on ArtStation"
```

---

*由 Autopilot Agent 自動生成*
