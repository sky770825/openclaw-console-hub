# API Monitor Skill 技術規格

## 任務資訊
- **任務 ID**: t1770909566680
- **名稱**: [CoDEX] API Monitor Skill
- **描述**: 定時 Health Check、回應追蹤、狀態碼監測
- **執行時間**: 2026-02-13T03:05:38+08:00
- **狀態**: ✅ 完成

## 功能規格

### 核心功能
1. **定時 Health Check**
   - 支援 Cron 表達式設定檢查頻率
   - 可配置多個端點同時監測
   - 支援 GET/POST/PUT/DELETE 等方法

2. **回應追蹤**
   - 記錄 Response Time (P50/P95/P99)
   - 追蹤 Response Body 內容變化
   - 儲存歷史回應數據

3. **狀態碼監測**
   - 監測 HTTP Status Code
   - 設定預期狀態碼範圍
   - 異常狀態告警通知

### 技術架構
```
api-monitor-skill/
├── src/
│   ├── index.ts          # Skill 入口
│   ├── monitor.ts        # 核心監測邏輯
│   ├── scheduler.ts      # 排程管理
│   ├── storage.ts        # 數據儲存
│   └── notifier.ts       # 通知系統
├── config/
│   └── default.yaml      # 預設配置
├── tests/
│   └── monitor.test.ts   # 單元測試
└── SKILL.md              # 使用文件
```

### API 設計
```typescript
interface MonitorConfig {
  name: string;
  url: string;
  method: 'GET' | 'POST' | 'PUT' | 'DELETE';
  headers?: Record<string, string>;
  body?: string;
  cron: string;           // 檢查頻率
  expectedStatus: number; // 預期狀態碼
  timeout: number;        // 請求超時 (ms)
  retries: number;        // 重試次數
}

interface MonitorResult {
  timestamp: Date;
  url: string;
  statusCode: number;
  responseTime: number;
  success: boolean;
  error?: string;
}
```

### 資料儲存
- 使用 SQLite 儲存監測歷史
- 自動清理 30 天前的舊數據
- 支援匯出 CSV/JSON

### 通知機制
- Telegram Bot 通知
- Discord Webhook
- Email (SMTP)
- 支援自定義 Webhook

### 告警條件
- 連續失敗 N 次
- Response Time 超過閾值
- Status Code 非預期
- SSL 憑證即將過期

## 實作建議
1. 使用 `node-cron` 處理排程
2. 使用 `axios` 或 `fetch` 發送請求
3. 使用 `better-sqlite3` 本地儲存
4. 遵循 OpenClaw Skill 規範封裝

## 相依套件
```json
{
  "dependencies": {
    "node-cron": "^3.0.3",
    "axios": "^1.6.0",
    "better-sqlite3": "^9.4.0"
  }
}
```

---
*由 Autopilot 自動生成*
